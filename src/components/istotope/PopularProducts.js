import FilterableProducts from "./FilterableProducts";

const PopularProducts = () => (
  <FilterableProducts
    subTitle="Popular Products"
    title="Popular Products"
    gridClass="popular-products-active"
  />
);

export default PopularProducts;
