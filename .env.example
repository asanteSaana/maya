# Server-only. Never prefix these with NEXT_PUBLIC_ — they would be inlined
# into the browser bundle. All backend calls go through /api/maya/* instead.
MAYA_API_BASE_URL=https://ecommerce-backend-9tly.onrender.com
MAYA_API_KEY=
